/** @type {import('next').NextConfig} */
const nextConfig = {
  basePath: "/WebHealthTracker",
  output: "export",
  reactStrictMode: true,
};

// next.config.js
module.exports = {
  output: "export",
};

export default nextConfig;
