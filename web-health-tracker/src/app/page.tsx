import React from "react";
import Layout from "./layout";
import MainDashboard from "./ui/main-dashboard";

const Home: React.FC = () => {
  return (
    <Layout>
      <MainDashboard />
    </Layout>
  );
};

export default Home;
